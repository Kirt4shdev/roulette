# =====================================================
# CONFIGURACIÓN DE PRODUCCIÓN - Quiz Roulette
# =====================================================
# Copia este archivo a .env en el servidor antes de desplegar
# cp .env.production .env
# 
# ⚠️ IMPORTANTE: Cambia las contraseñas antes de usar en producción
# =====================================================

# -----------------------------------------------------
# Base de datos PostgreSQL
# -----------------------------------------------------
POSTGRES_USER=quiz_user
POSTGRES_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURA
POSTGRES_DB=quiz_db

# -----------------------------------------------------
# Autenticación Admin
# -----------------------------------------------------
# Token para acceder al panel de administración
# Usa un token largo y aleatorio en producción
ADMIN_TOKEN=CAMBIAR_POR_TOKEN_SEGURO_Y_LARGO

# -----------------------------------------------------
# Entorno
# -----------------------------------------------------
NODE_ENV=production

# -----------------------------------------------------
# URLs (no cambiar a menos que uses otro dominio)
# -----------------------------------------------------
FRONTEND_URL=https://dilus.mchdev.es

